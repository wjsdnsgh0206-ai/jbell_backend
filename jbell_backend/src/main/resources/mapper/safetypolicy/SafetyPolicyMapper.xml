<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jbell.safetypolicy.mapper.SafetyPolicyMapper">

    <sql id="searchCriteria">
        AND content_type = 'SAFETY_POLICY'
        <if test="keyword != null and keyword != ''">
            AND (title LIKE CONCAT('%', #{keyword}, '%') OR source LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="visibleYn != null and visibleYn != ''">
            AND visible_yn = #{visibleYn}
        </if>
    </sql>

    <select id="selectSafetyPolicyList" resultType="jbell.safetypolicy.dto.SafetyPolicyDTO">
        SELECT 
            content_id, title, source, created_at, visible_yn, reg_type
        FROM content
        WHERE 1=1
        <include refid="searchCriteria"/>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countSafetyPolicyList" resultType="long">
        SELECT count(*) FROM content WHERE 1=1 <include refid="searchCriteria"/>
    </select>

    <select id="selectSafetyPolicyDetail" resultType="jbell.safetypolicy.dto.SafetyPolicyDTO">
        SELECT 
            content_id, title, body, source, content_link, visible_yn, created_at, reg_type
        FROM content
        WHERE content_id = #{contentId} AND content_type = 'SAFETY_POLICY'
    </select>

    <insert id="insertSafetyPolicy" parameterType="jbell.safetypolicy.dto.SafetyPolicyDTO" 
            useGeneratedKeys="true" keyProperty="contentId">
        INSERT INTO content (
            title, body, source, content_link, visible_yn, 
            content_type, reg_type, user_id, ordering
        ) VALUES (
            #{title}, #{body}, #{source}, #{contentLink}, #{visibleYn}, 
            'SAFETY_POLICY', 'A', #{userId}, 0
        )
    </insert>

    <update id="updateSafetyPolicy">
        UPDATE content
        SET title = #{title},
            body = #{body},
            source = #{source},
            content_link = #{contentLink},
            visible_yn = #{visibleYn},
            last_update_date = NOW()
        WHERE content_id = #{contentId}
    </update>
</mapper>