<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jbell.auth.mapper.UserMapper"> 


<insert id="insertUser" parameterType="jbell.auth.dto.SignupRequest">
    INSERT INTO user (
        user_id, user_password, user_name, user_birth_date, user_email, 
        user_gender, user_status, user_grade, user_residence_area
    ) VALUES (
        #{userId}, 
        #{userPw}, 
        #{name},        #{birthDate},   #{email},       #{userGender}, 
        TRUE, 
        'USER', 
        #{residenceArea}
    )
</insert>



    <select id="existsByUserId" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM user WHERE user_id = #{userId}
    </select>

</mapper>